// All the code in JS runs sequentially
// But, the events are invoked on an activity
// Events Listener provides the ability of propagation ability
// Bubbling: Where the activity bubbles up. This means that the most inner event invokes first (when false)
// Capturing: In this the outermost activity invokes first (when true)